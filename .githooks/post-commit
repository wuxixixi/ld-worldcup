#!/bin/bash
# LD看世界杯 — 自动推送到 GitHub
BRANCH=$(git rev-parse --abbrev-ref HEAD)
echo "[post-commit] auto-pushing to origin/$BRANCH ..."
# 用 GIT_TERMINAL_PROMPT=0 避免挂起；stderr 合并到日志
GIT_TERMINAL_PROMPT=0 git push origin "$BRANCH" 2>&1 | tail -5
exit 0
